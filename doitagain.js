function myArray () {
    const numChild = [1,2,3,4,5,6,7,8];

    for (let i = 0; i < numChild.length; i++) {
        const result = numChild[i]
        console.log (result);
    }
}

myArray()


function arrayCounter (array) {
    for(let i = 0; i < array.length; i++) {
        const result = [i];
        console.log[result];
    }
}

const myShirt = ["cotton","silk"];
const myLorezo = ["schalltt", "drift"]
arrayCounter(myLorezo);